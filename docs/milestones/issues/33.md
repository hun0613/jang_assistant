# #33 Supabase 프로젝트 세팅 및 Next.js 연동

## 이슈 정보

| 항목 | 내용 |
|------|------|
| 마일스톤 | Phase 1 - 백엔드 도입 및 데이터 영속화 |
| 목표 | Supabase 프로젝트와 Next.js를 연동하여 백엔드 인프라 기반을 구축한다 |
| 브랜치 | `feat/#33` |

## 작업 단계

### Step 1. Supabase 패키지 설치
- `@supabase/supabase-js@2.100.0` 설치 (pnpm)
- 상태: ✅ 완료

### Step 2. 환경변수 설정
- `.env.local`에 `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` 설정
- `.env.example` 파일 생성 (키 값 없이 변수명만)
- `.env*.local`은 기존 `.gitignore`에 포함되어 있어 별도 처리 불필요
- 상태: ✅ 완료

### Step 3. Supabase Client 유틸 구성
- `src/utils/supabaseUtil.ts` 생성
- `createClient`로 Supabase 클라이언트 인스턴스 생성 및 export
- 기존 유틸 파일(domUtil, storageUtil)과 동일한 위치에 배치
- 상태: ✅ 완료

### Step 4. 연동 확인
- `pnpm build` 정상 완료 확인
- Supabase Client가 포함된 상태에서 빌드 에러 없음
- 상태: ✅ 완료

## 진행 기록

### 2026-03-25
- Step 1~3 완료: Supabase 패키지 설치, 환경변수 설정, Client 유틸 구성
- Supabase 프로젝트: jang-assistant (Region: Northeast Asia - Korea)
- 기술 선택 배경: localStorage 의존성 탈피를 위해 Supabase BaaS 도입
